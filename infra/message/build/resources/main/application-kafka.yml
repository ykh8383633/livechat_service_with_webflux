spring:
  kafka:
    properties:
      spring:
        json:
          value.default.type=
    bootstrapServers: localhost:9093
    topics:
      inMessage: in-message
      outMessage: out-message
      doneSpamProcess: done-spam-process

